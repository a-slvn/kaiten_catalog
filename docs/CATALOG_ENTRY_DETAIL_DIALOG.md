# CatalogEntryDetailDialog — модальное окно записи каталога

Модальное окно для просмотра детальной информации по отдельной записи каталога: полей, связанных карточек (сделок) и управления записью.

---

## Где открывается

| Точка входа | Как открывается |
|---|---|
| Страница каталога (`CatalogDetailPage`) | Клик по строке записи в таблице |
| Карточка сделки (`DealModal`) | Клик по карточке записи каталога в коллапсе поля |

---

## User story

| Задачи (Как пользователь я...) | Цель (Чтобы...) | Критерии готовности | Артефакты |
|---|---|---|---|
| Хочу посмотреть данные записи каталога | Быстро получить всю информацию не переходя на другую страницу | Модалка открывается и показывает заполненные поля записи | Модальное окно с вкладкой «Обзор» |
| Хочу увидеть связанные сделки | Понять, с какими сделками связана запись | Отображается список карточек сделок с суммой и датой | Секция «Связанные карточки» и вкладка «Карточки» |
| Хочу открыть сделку из модалки | Перейти к нужной сделке в один клик | Клик по карточке сделки закрывает модалку и открывает DealModal | Кликабельные карточки сделок |
| Хочу открыть сделку в новой вкладке | Не терять контекст текущего окна | Cmd+Click или ПКМ → «Открыть в новой вкладке» | Хэш-навигация `#deal=ID` |
| Хочу отредактировать запись | Внести изменения в данные | Кнопка «Редактировать» открывает форму редактирования | Кнопка в футере модалки |
| Хочу удалить запись | Удалить ненужную запись без перехода на страницу каталога | Кнопка «Удалить» показывает диалог подтверждения | Диалог подтверждения удаления |

---

## Use Case

| Use Case | Описание |
|---|---|
| Открыть модальное окно | **Требования для FE:**<br>Модалка открывается поверх текущего экрана. Размер — `maxWidth="md"`, `fullWidth`. Минимальная высота — 72vh, максимальная — 90vh. Заголовок — `displayValue` записи (первое значение, используемое как название). Справа в заголовке — кнопка закрытия ×. |
| Просмотр полей (вкладка «Обзор») | **Требования для FE:**<br>Показываем только заполненные поля. По умолчанию отображаются первые 6 полей. Если заполненных полей больше 6 — показываем кнопку «Показать все поля» / «Свернуть поля». Каждое поле отображается с иконкой своего типа (см. раздел «Типы полей»). |
| Просмотр связанных карточек на вкладке «Обзор» | **Требования для FE:**<br>Секция «Связанные карточки» показывает до 3 последних карточек. Справа от заголовка секции — кнопка «Все карточки», которая переключает на вкладку «Карточки». Если связанных карточек нет — показываем текст «Нет связанных карточек». |
| Вкладка «Карточки» | **Требования для FE:**<br>Показываем все связанные карточки постранично по 15 штук. Кнопка «Показать еще N» подгружает следующую порцию. Счётчик в заголовке вкладки показывает общее количество: `Карточки (N)`. |
| Открыть сделку | **Требования для FE:**<br>Клик по карточке сделки — закрывает модалку записи каталога и открывает `DealModal` для выбранной сделки. `Cmd+Click` (Mac) / `Ctrl+Click` (Win) — открывает сделку в новой вкладке через URL `#deal=ID`. ПКМ на карточке сделки — показывает контекстное меню с пунктом «Открыть в новой вкладке». |
| Редактировать запись | **Требования для FE:**<br>Кнопка «Редактировать» в футере открывает форму редактирования (`CreateCatalogEntryDialog` в режиме `editMode`). Модалка просмотра при этом закрывается. |
| Удалить запись | **Требования для FE:**<br>Кнопка «Удалить» в футере открывает диалог подтверждения. В диалоге: название записи, кнопки «Отмена» и «Удалить». При подтверждении — запись удаляется, модалка закрывается. Действие необратимо. |

---

## Структура модалки

```
┌──────────────────────────────────────────────────────┐
│  [Название записи]                              [×]  │
├──────────────────────────────────────────────────────┤
│       ОБЗОР          │        КАРТОЧКИ (N)           │
├──────────────────────────────────────────────────────┤
│                                                      │
│  ┌─ Ключевые поля ─────────────────────────────┐    │
│  │  🔤 Название                                 │    │
│  │     Значение                                 │    │
│  │  📞 Телефон                                  │    │
│  │     +7...                                    │    │
│  │  [Показать все поля ▼]                       │    │
│  └──────────────────────────────────────────────┘    │
│                                                      │
│  ┌─ Связанные карточки ──────── [Все карточки] ┐    │
│  │  ● ООО Меридиан  #123 · 10.01.2025  112 000₽│    │
│  │  ● ООО Олимп                         68 000₽│    │
│  └──────────────────────────────────────────────┘    │
│                                                      │
├──────────────────────────────────────────────────────┤
│  🗑 Удалить             [Закрыть]  [✏ Редактировать] │
└──────────────────────────────────────────────────────┘
```

---

## Вкладки

### Обзор

Содержит две секции:

**1. Ключевые поля**

- Показываем только поля с заполненными значениями
- По умолчанию — первые 6 полей
- Если полей больше 6 → кнопка «Показать все поля» / «Свернуть поля»
- Каждое поле: иконка типа + название + значение

**2. Связанные карточки**

- До 3 последних карточек сделок
- Кнопка «Все карточки» → переключает на вкладку «Карточки»
- Если карточек нет → «Нет связанных карточек»

---

### Карточки (N)

- Полный список всех связанных сделок
- Пагинация: по 15 штук
- Кнопка «Показать еще N» (где N — остаток)
- Если карточек нет → «Нет связанных карточек»

**Как формируется список связанных карточек:**

Запись каталога может содержать поля типа `reference` — ссылки на контакты или другие сущности. Система собирает сделки, связанные:
1. Непосредственно с самой записью каталога
2. С записями из полей типа `reference` (например, контактами компании)

Итоговый список — объединение всех найденных сделок без дубликатов.

---

## Типы полей и их отображение

| Тип поля | Иконка | Отображение |
|---|---|---|
| `text` (по умолчанию) | Tt | Простой текст |
| `email` | ✉ | Кликабельная ссылка `mailto:` |
| `phone` | 📞 | Кликабельная ссылка `tel:` |
| `url` | 🔗 | Внешняя ссылка, открывается в новой вкладке |
| `numeric` | # | Число, отформатированное по `ru-RU` (разряды) |
| `select` | ≡ | Chip синего цвета |
| `multiselect` | ≡ | Несколько Chip'ов синего цвета |
| `reference` | 👤 | Карточка контакта (`ReferenceCard`), кликабельная |
| `catalog_ref` | 📁 | Карточка записи каталога (`CatalogRefCard`), кликабельная |

Если значение не заполнено — показываем курсивный текст «Не указано» / «Не выбрано».

Обязательные поля отмечены красной звёздочкой `*` рядом с названием.

---

## Карточка сделки

```
┌──────────────────────────────────────────────────┐
│  ●  ООО Меридиан                       112 000 ₽ │
│     #58998179 · 10.01.2025                        │
└──────────────────────────────────────────────────┘
```

| Элемент | Описание |
|---|---|
| Цветной кружок | Аватар сделки, цвет берётся из `deal.avatarColor` |
| Заголовок | `deal.title` — название сделки |
| Подстрока | Номер заказа `deal.orderNumber` и дата создания `deal.createdDate` в формате `dd.mm.yyyy` |
| Сумма | `deal.amount` в рублях, форматирование по `ru-RU` |

### Интерактивность карточки сделки

| Действие | Результат |
|---|---|
| Клик | Закрывает модалку записи каталога → открывает `DealModal` для этой сделки |
| `Cmd+Click` (Mac) / `Ctrl+Click` (Win) | Открывает сделку в новой вкладке по URL `#deal={id}` |
| ПКМ | Контекстное меню с пунктом «Открыть в новой вкладке» |
| `Enter` / `Space` (фокус) | Аналогично обычному клику |

---

## Карточка контакта (ReferenceCard)

Используется для полей типа `reference`. Показывает данные из записи справочника.

```
┌───────────────────────────────────┐
│  Андрей                      ↗    │
│  Менеджер · +79178151515          │
└───────────────────────────────────┘
```

Метаданные в подстроке определяются автоматически из полей записи по приоритету:
1. Должность + Телефон
2. Email (если нет телефона)
3. Первые два любых заполненных поля

Если запись не найдена по ID — показывается сообщение «Запись не найдена (ID: ...)».

---

## Карточка записи каталога (CatalogRefCard)

Используется для полей типа `catalog_ref`.

```
┌───────────────────────────────────┐
│  📁 Кайтен                        │
│     Название: Кайтен | ИИН: 111.. │
└───────────────────────────────────┘
```

Показывает до 3 первых полей записи в формате `Поле: Значение | Поле: Значение`.

---

## Футер модалки

| Кнопка | Позиция | Поведение |
|---|---|---|
| 🗑 Удалить | Слева | Открывает диалог подтверждения удаления |
| Закрыть | Справа | Закрывает модалку без изменений |
| ✏ Редактировать | Справа (primary) | Открывает форму редактирования записи |

Футер закреплён (`position: sticky, bottom: 0`) — всегда виден при прокрутке.

---

## Диалог подтверждения удаления

Открывается при нажатии «Удалить».

```
Удалить запись?

Это действие нельзя отменить. Запись "Кайтен" будет
удалена без возможности восстановления.

                              [Отмена]  [Удалить]
```

| Кнопка | Поведение |
|---|---|
| Отмена | Закрывает диалог, возвращается к модалке записи |
| Удалить | Удаляет запись → закрывает оба окна |

---

## Хэш-навигация для сделок

Сделки адресуемы через хэш URL:

```
http://localhost:5173/#deal={dealId}
```

- При открытии страницы с таким хэшем — автоматически открывается `DealModal` для соответствующей сделки
- При закрытии `DealModal` — хэш очищается
- Поддерживаются кнопки «Назад» / «Вперёд» браузера

---

## Сброс состояния при смене записи

При изменении `entryId` (открытии другой записи) сбрасываются:
- Активная вкладка → «Обзор»
- Флаг «Показать все поля» → скрыто
- Счётчик видимых карточек → 15

---

## Компонент: пропсы

| Проп | Тип | Обязательный | Описание |
|---|---|---|---|
| `open` | `boolean` | ✓ | Открыта ли модалка |
| `onClose` | `() => void` | ✓ | Callback закрытия |
| `catalog` | `Catalog` | ✓ | Объект каталога, к которому относится запись |
| `entryId` | `string` | ✓ | ID записи для отображения |
| `onEdit` | `() => void` | ✓ | Callback для открытия формы редактирования |
| `onOpenCatalogEntry` | `(catalogId, entryId) => void` | — | Открытие другой записи каталога (из полей `reference`/`catalog_ref`) |
| `onOpenDeal` | `(dealId) => void` | — | Открытие сделки при клике на карточку |
