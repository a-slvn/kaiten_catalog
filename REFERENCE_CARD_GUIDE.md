# Работа с полями справочника в карточках - Руководство

## Что изменилось

### 1. **Маска телефона** 📞
Теперь поля типа "Телефон" автоматически форматируются с маской `+7 (999) 999-99-99`

**Пример**:
- Вводите: `9001112233`
- Видите: `+7 (900) 111-22-33`

### 2. **Выбор записей из справочника** 🔗
В карточках теперь работают поля типа "Справочник" - можно выбирать Компанию, Клиента и т.д.

**До**: Было просто "Выберите из справочника" (не работало)
**Сейчас**: Полноценный выпадающий список с записями

### 3. **Создание новой записи прямо из выпадающего списка** ✨
Если в справочнике нет нужной записи - создайте её прямо отсюда!

**Как работает**:
1. Открываете поле "Клиент"
2. В списке видите `+ Создать Клиент`
3. Кликаете → открывается модалка создания
4. Заполняете поля клиента (Имя, Телефон, Email, Компания)
5. Нажимаете "Создать"
6. Новый клиент автоматически выбирается в поле!

### 4. **Редактирование справочников** ✏️

#### Из карточки справочника:
- Кнопка "Редактировать справочник" (иконка карандаша) в заголовке
- Открывается модалка редактирования
- Можно изменить название, добавить/удалить поля

#### Со страницы "Пользовательские поля":
- Кнопка "Редактировать" в таблице справочников
- Та же модалка редактирования

## Пример использования

### Создание структуры Компания → Клиент → Сделка

#### Шаг 1: Создайте справочник "Компания"
1. Страница "Пользовательские поля"
2. "Добавить справочник"
3. Тип: Справочник
4. Название: "Компания"
5. Поля:
   - Название (текст) ✓ обязательное
   - ИНН (число)
6. Сохранить

#### Шаг 2: Создайте справочник "Клиент" со связью на Компанию
1. "Добавить справочник"
2. Название: "Клиент"
3. Поля:
   - Имя (текст) ✓ обязательное
   - Телефон (телефон)
   - Email (email)
   - **Компания (справочник)**:
     - Целевой справочник: "Компания"
     - ☑ Каскадная фильтрация
4. Сохранить

#### Шаг 3: Используйте в сделке
1. Откройте любую сделку
2. Добавьте поле "Компания"
   - Если компаний нет → нажмите `+ Создать Компания`
   - Введите: Название: "ООО Ромашка", ИНН: "1234567890"
   - Нажмите "Создать"
3. Добавьте поле "Клиент"
   - В списке автоматически только клиенты ООО Ромашка (каскадная фильтрация!)
   - Если клиентов нет → нажмите `+ Создать Клиент`
   - Введите: Имя: "Иванов Иван", Телефон: "9001112233"
   - Поле "Компания" уже заполнено "ООО Ромашка"!
   - Нажмите "Создать"

## Компоненты

### CreateReferenceEntryDialog
Модалка для создания новой записи в справочнике

**Props**:
- `open: boolean` - открыта ли модалка
- `referenceDefinitionId: string | null` - ID справочника
- `onClose: () => void` - закрыть модалку
- `onCreate: (entryId: string) => void` - callback с ID созданной записи

### ReferenceFieldSelect (обновлен)
Умный селектор записей с автофильтрацией

**Новые возможности**:
- Опция `+ Создать {название справочника}` всегда первая в списке
- Callback `onCreateNew?: () => void` для создания новой записи
- Фиолетовый цвет и иконка "+" для опции создания

### ReferenceFieldCard (обновлен)
Карточка справочника с полями

**Новые возможности**:
- Поля типа "reference" теперь используют ReferenceFieldSelect
- Маска телефона для полей типа "phone"
- Кнопка "Редактировать справочник"
- Интеграция с CreateReferenceEntryDialog

## Технические детали

### Маска телефона
```typescript
import InputMask from 'react-input-mask';

<InputMask mask="+7 (999) 999-99-99" value={value} onChange={onChange}>
  {(inputProps: any) => <TextField {...inputProps} />}
</InputMask>
```

### Создание записи из выпадающего списка
```typescript
// В ReferenceFieldSelect
<ReferenceFieldSelect
  fieldDef={fieldDef}
  value={value}
  onChange={setValue}
  onCreateNew={() => setCreateDialogOpen(true)} // ← новый callback
/>

// CreateReferenceEntryDialog
<CreateReferenceEntryDialog
  open={createDialogOpen}
  referenceDefinitionId={targetRefId}
  onClose={() => setCreateDialogOpen(false)}
  onCreate={(entryId) => {
    setValue(entryId);  // автоматически выбрать созданную запись
    setCreateDialogOpen(false);
  }}
/>
```

### Редактирование справочника
```typescript
// AddCustomFieldModal в режиме редактирования
<AddCustomFieldModal
  open={modalOpen}
  onClose={handleClose}
  editMode={true}  // ← режим редактирования
  fieldToEdit={selectedField}  // ← справочник для редактирования
  onUpdate={updateFieldDefinition}  // ← callback обновления
/>
```

## FAQ

**Q: Как добавить новое поле в существующий справочник?**
A: Нажмите кнопку "Редактировать справочник" → "Добавить поле" → Заполните название и тип → Сохранить

**Q: Почему в поле "Клиент" не видно всех клиентов?**
A: Работает каскадная фильтрация. Сначала выберите Компанию, затем в поле Клиент будут только клиенты этой компании.

**Q: Можно ли создать запись без каскадной фильтрации?**
A: Да, при создании поля типа "Справочник" снимите галочку "Каскадная фильтрация"

**Q: Как изменить маску телефона?**
A: В файле `ReferenceFieldCard.tsx` найдите `mask="+7 (999) 999-99-99"` и измените на нужную

## Пример полного сценария

```
Задача: Создать сделку с клиентом из новой компании

1. Открыть сделку
2. Нажать "+", выбрать поле "Компания"
3. В выпадающем списке → "+ Создать Компания"
4. Ввести: ООО "Василёк", ИНН 9876543210 → Создать
5. Нажать "+", выбрать поле "Клиент"
6. В выпадающем списке → "+ Создать Клиент"
   (поле Компания уже = "ООО Василёк"!)
7. Ввести: Сидоров Сергей, +79005556677 → Создать
8. Готово! Сделка связана с компанией и клиентом
```

Теперь можно:
- Кликнуть на "ООО Василёк" → увидеть всех клиентов и сделки компании
- Кликнуть на "Сидоров Сергей" → увидеть его компанию и все сделки
- Отфильтровать сделки по компании или клиенту
